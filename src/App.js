import React, { Component } from "react";
import Header from "components/Header";
import { initStyle } from "components/common/init";

class App extends Component {
  render() {
    return (
      <div>
        <Header />
      </div>
    );
  }
}

export default App;
